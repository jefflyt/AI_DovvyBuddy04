[pytest]
asyncio_mode = auto
addopts = --import-mode=importlib
python_files = tests/**/*.py test_*.py *_test.py
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests requiring real services (network, database)
